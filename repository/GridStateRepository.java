package com.gridweaver.gridweaver.repository;

import com.gridweaver.gridweaver.model.GridState;
import org.springframework.data.jpa.repository.JpaRepository;

public interface GridStateRepository extends JpaRepository<GridState, Long> {
}